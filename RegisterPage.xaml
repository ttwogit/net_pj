<Page
    x:Class="net_pj.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:net_pj"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Padding="40" VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Đăng ký" FontSize="24" Margin="0,0,0,20"/>

            <TextBox x:Name="NewUsernameTextBox" Header="Tài khoản" Margin="0,10,0,0" />
            <TextBox x:Name="EmailTextBox" Header="Email" Margin="0,10,0,0"/>
            <PasswordBox x:Name="NewPasswordBox" Header="Mật khẩu" Margin="0,10,0,0"/>
            <Button Content="Đăng ký" Click="RegisterButton_Click" Margin="0,10,0,0" HorizontalAlignment="Center"/>

            <TextBlock x:Name="MessageTextBlock" Margin="0,10,0,0"/>
            <Button Content="Đã có tài khoản? Đăng nhập" Click="LoginButton_Click" Margin="0,10,0,0"/>

        </StackPanel>
    </Grid>
</Page>
