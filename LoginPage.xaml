<Page
    x:Class="net_pj.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:net_pj"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Padding="40" VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <TextBlock Text="Đăng nhập" FontSize="24" Margin="0,0,0,10"/>

            <TextBox x:Name="UsernameTextBox" Header="Tài khoản"/>
            <PasswordBox x:Name="PasswordBox" Header="Mật khẩu" Margin="0,10,0,0"/>

            <Button Content="Đăng nhập" Click="LoginButton_Click" Margin="0,10,0,0"/>
            <TextBlock x:Name="MessageTextBlock" Margin="0,10,0,0"/>

            <Button Content="Chưa có tài khoản? Đăng ký" Click="RegisterButton_Click" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>
