<Page
    x:Class="net_pj.UserInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:net_pj"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Padding="20">
            <TextBlock x:Name="UsernameTextBlock" FontSize="20" Margin="0,10"/>
            <TextBlock x:Name="TokenTextBlock" FontSize="20" Margin="0,10"/>
            <TextBlock Text="Món đã gọi:" FontSize="20" Margin="0,10"/>
            <ListView x:Name="FoodListView" Margin="0,10"/>
            <Button Content="🔙 Trở về menu chính"
        Click="BackButton_Click"
        Background="Gray"
        Foreground="White"
        Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Page>
